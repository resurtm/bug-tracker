{% extends 'layouts/base.html.twig' %}

{% block title %}Ticket: {{ ticket.title }} &mdash; {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-md-12 col-md-offset-0">

            <h1>Ticket: {{ ticket.title }}</h1>
            <p class="lead">{{ ticket.description }}</p>

        </div>
    </div>
{% endblock %}
